spring.application.name=scm
server.port=8081

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/scm_db
spring.datasource.username=root
spring.datasource.password=PRATHAM152005
# Specify the MySQL JDBC driver class name
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
# Show SQL statements in the console
# spring.jpa.show-sql=true
# # Automatically create/update the database schema based on the entities
# spring.jpa.hibernate.ddl-auto=update
# # Specify the Hibernate dialect for MySQL
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect